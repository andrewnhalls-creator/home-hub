# Next Steps

## Outstanding
- Visual QA of finanzas pager on the deployed production URL — confirm the named tab chip scrolls correctly to centre when switching pages (commit `81f94b5` is now live)
- Verify RLS on the `debts` table with a second user account

## No outstanding code work
All finance UI fixes are committed, pushed, and deploying. AI assistant is fully wired.
